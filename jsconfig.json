{
    "compilerOptions": {
      "baseUrl": "./",  // 项目的根目录
      "paths": {
        "@/*": ["src/*"]  // 定义路径别名
      }
    },
    "exclude": ["node_modules", "dist"]  // 排除 node_modules 和 dist 文件夹
  }

  
  